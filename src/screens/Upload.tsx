export function Upload() {
  return (
    <section className="screen" aria-labelledby="upload-heading">
      <h1 id="upload-heading">Upload</h1>
      <p>
        Add one or more PDFs and declare where the answers live so the engine
        never has to guess the answer form.
      </p>
    </section>
  )
}
