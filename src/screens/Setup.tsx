export function Setup() {
  return (
    <section className="screen" aria-labelledby="setup-heading">
      <h1 id="setup-heading">Setup</h1>
      <p>
        Paste one free provider API key first, validate it clearly, and defer
        extra keys until later.
      </p>
    </section>
  )
}
