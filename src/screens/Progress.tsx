export function Progress() {
  return (
    <section className="screen" aria-labelledby="progress-heading">
      <h1 id="progress-heading">Progress</h1>
      <p>
        Show per-file and overall progress with quota-aware pauses that resume
        without making the app feel broken.
      </p>
    </section>
  )
}
