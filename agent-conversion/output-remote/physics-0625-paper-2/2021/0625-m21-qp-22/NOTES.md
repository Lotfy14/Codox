# 0625/22 Physics — Paper 2 Multiple Choice (Extended), February/March 2021

**Source:** `0625_m21_qp_22.pdf` (Cambridge IGCSE Physics 0625/22), with the
published mark scheme `0625_m21_ms_22` paired in as answer-key pages.

**Converted by:** claude-opus-5

## What the document is

A 40-question multiple-choice paper. The paper itself is 16 pages
(bundle pages 1–16; page 1 is the instructions cover, questions run on pages
2–16). The mark scheme is appended as bundle pages 17–19, marked
`"role": "answer-key"` — page 17 is its cover, page 18 carries questions 1–28
and page 19 carries questions 29–40.

Every question has exactly four options, A–D. The paper states "There are
**forty** questions on this paper" and the total mark is 40 — the 40 extracted
questions match that claim exactly, one per printed number, none skipped.

## Where the answers came from

**A separate answer key**, not marks on the paper. The question paper is a
clean unmarked print with no ticks, no margin letters and no answer column.
The mark scheme's `Question | Answer | Marks` table gives one printed letter
per question.

All 40 answers are `"source": "extracted"`, each with an `evidence` string
naming the key page and row (e.g. `mark scheme page 18, row 7: answer C`).
Nothing was reasoned and nothing was left blank. Spot-checks against the
physics (Q3 = 60 km / 20 min = 50 m/s = C; Q5 = 130 g / 10 cm³ = 13 g/cm³ = C;
Q6 = 0.80 × (4.0/60) = 0.053 N = A; Q22 reflection = incidence = 38° = A)
confirm the key rows line up with the question numbers, so the table is not
offset.

## Options printed as pictures, not as a list

Eleven questions print no text option list — the choices are lettered rows of a
table, or four lettered pictures. Per QUALITY.md these carry
`["A","B","C","D"]` as options and the table/pictures ship as figures, so the
tutor picks a letter and reads the row off the image exactly as a candidate
does:

- **Q9, Q17, Q23, Q26, Q30, Q34** — "which row" tables (each also has its own
  diagram, cropped separately: two figures, diagram first, table second)
- **Q25, Q27, Q31** — "which row" tables with no accompanying diagram
- **Q35** — four labelled truth tables (plus the logic-gate circuit diagram)
- **Q37** — four labelled voltage-against-time graphs

No table row was flattened into option text.

## Figures

30 figures cropped, every one opened and checked for both failure directions
(clipped label / bleeding into neighbouring or trailing text). Seven crops were
re-cut after looking: fig-04, fig-10, fig-12 and fig-19 were widened where a
table border or the iron nails were clipped; fig-05, fig-09, fig-13, fig-26 and
fig-28 were tightened because a trailing line of body text sat inside the crop.

"One picture, one figure" is honoured throughout — a question with a diagram
*and* an option table gets two separate crops (Q9, Q17, Q23, Q26, Q30, Q34,
Q35), and Q29's two rows of coil diagrams (current on / current off) are two
figures because printed text separates them.

Q40's data table and Q28's / Q38's numbered statement lists are different
cases: Q40's table of count-rate readings is a picture (cropped as fig-30),
while Q28 and Q38 print their statements as ordinary text, so those stay text
with their line breaks preserved.

## Transcription notes

- Question numbering (`1`, `2`, …) and option letters (`A`, `B`, …) are
  stripped, as Codox numbers them itself.
- Unit spacing is normalised to the ordinary reading (`30 m/s`, `10 cm³`,
  `8.0 Ω`) — the PDF's text layer renders these with thin spaces
  (`30 m / s`). No wording was changed.
- Q11's options are printed as stacked fractions; they are written linearly as
  `3P/8`, `3P/2`, `8P/3`, `6P`. This is a real printed option list, so it stays
  text rather than becoming a figure.
- Q28 and Q38 keep their statement lists on separate lines inside the stem, per
  QUALITY.md, so the item boundaries survive.
- `year` is `"2021"` on every question, from the paper's own
  "February/March 2021" header. The document states no topic taxonomy, so
  `topic`/`subtopic` are empty and `topics[]` is empty — the tutor can match
  against their own list after import.

## Left for the tutor / unresolved

Nothing is flagged and nothing is blank. Every question has its own `box`; none
crosses a page break, so no question fell back to a whole-page region.

One judgement worth a glance: the eleven `["A","B","C","D"]` questions above
read as bare letters in Review until the tutor looks at the attached table
crop. That is the intended shape for a "which row is correct?" question, but it
is the thing to check first if any of them looks wrong.

Validator: **0 errors, 0 warnings** — 40 questions, 19 pages, 30 figures,
40 answers read from the document.
