# 0625/22 — Cambridge IGCSE Physics, Paper 2 Multiple Choice (Extended), May/June 2021

**Source:** `0625_s21_qp_22.pdf` (16 pages) with the published UCLES mark scheme
appended by `agent-prepare` as pages 17–19 (`"role": "answer-key"`).

**Extracted by:** claude-opus-5

## What the document is

A standard Cambridge multiple-choice paper. The cover (page 1) states
"There are **forty** questions on this paper" and "For each question there are
four possible answers **A**, **B**, **C** and **D**" — all 40 are present and
each has exactly four options. Questions run over pages 2–16, numbered 1–40
continuously, no restart. There are no shared case stems and no
matching / true-false / extended-matching formats — every question is a plain
single-answer MCQ.

Every question was extracted. Count matches the document's own claim (40).

## Where the answers came from

A **separate answer key**: the published mark scheme, pages 17–19 of the bundle.
Page 18 is a `Question | Answer | Marks` table covering Q1–Q28; page 19 covers
Q29–Q40. Every row is a single printed capital letter, all legible, no
conflicts and no gaps.

All 40 answers are therefore `"source": "extracted"`, with `evidence` naming
the mark-scheme row and the key page it was read from. Nothing was reasoned
and nothing was left blank. The exam pages themselves carry **no** marks —
this is a clean, unwritten-on question paper — so the key is the only answer
source, exactly as it should be.

Spot-checks against physics (Q11 400 W × 15% = 60 W → A; Q13 ρghA = 200 kN → C;
Q18 ΔT = 7300/(2.0 × 913) = 4.0 °C → 24 °C → C; Q22 3.0e8 × sin33/sin55 =
2.0e8 → B; Q36 22 000 × 115/550 = 4600 V → C; Q39 two β decays → B;
Q40 120 → 60 counts/s at 2 minutes → B) all agree with the key, which
confirms the letter-to-index mapping is not offset anywhere.

## Options: printed lists vs. lettered pictures

13 questions print **no option list** — the choices are the lettered rows of a
table, or letters drawn into a diagram. Per QUALITY.md these carry
`["A","B","C","D"]` as options and the table/drawing ships as a figure, so the
tutor reads the row off the picture exactly as a candidate does:

- **Table rows** (`Which row …` / `Which combination …`): Q9, Q14, Q16, Q21,
  Q26, Q28, Q33, Q37, Q38
- **Letters inside a drawing**: Q2 (four falling balls), Q3 (four speed–time
  graphs), Q7 (four force arrows around a satellite), Q32 (four circuits)

No table was flattened into option text. The remaining 27 questions print a
real option list and keep it as text, verbatim, with the `A`/`B`/`C`/`D`
labels stripped.

Q25 introduces a numbered list of three statements inside its stem. That is
printed *text*, not a picture, so it stays in `question` with its line breaks
preserved (statements 1/2/3 on their own lines, blank lines separating the
three parts) — the options are the printed `1 and 3` / `1 only` / … list.

## Figures

27 crops, every one opened and checked after cropping — several were re-cropped
because a table's bottom border or a caption's descenders were clipped by a few
pixels (figs 6, 7, 9, 10, 14, 17, 18, 20, 24, 25) or because a diagram's bench
line sat exactly on the edge (figs 15, 16). None bleeds into a neighbouring
question.

Two questions get **more than one** figure, per the one-picture-one-figure rule:

- **Q26** — the loudspeaker/material diagram (`fig-13`) and the option table
  (`fig-14`) are cropped separately and both listed on the question.
- **Q28** — `diagram 1` (`fig-15`), `diagram 2` (`fig-16`) and the option table
  (`fig-17`) are three separate crops.

**Q39** is the one judgement call worth flagging: the decay series
`238/92 U → 234/90 Th → …` is typeset with stacked mass and proton numbers that
plain text cannot carry faithfully. It is cropped as `fig-26` and the two prose
sentences around it are the question text — the chain is not transcribed into
`question`, so the tutor reads it from the picture. Nothing is lost, but it is
a deliberate departure from "the document printed text, so keep it as text".

## Boxes

Every one of the 40 questions has its own `box` — none omitted, none falling
back to the whole page. Each box covers that question's stem, its own diagram
and option table, down to its last option, and stops short of the neighbour
above and below. Boxes span the full text column horizontally (x 68–940)
because the paper's justified stems run nearly to the right margin; several
were widened after a preview showed a stem clipped mid-sentence (Q5, Q10–Q14).

## Topics and year

The paper prints no topic taxonomy, so `topic` and `subtopic` are left blank
for the tutor to match against their own list in Codox. `year` is `"2021"`,
from the cover ("May/June 2021") and the running footer (`0625/22/M/J/21`).

## Left for the tutor

Nothing is blocked. No question is flagged, no answer is blank, and no answer
is waiting on approval — the whole paper should export straight away. The only
thing worth a glance is Q39's figure-only decay chain described above.
