# 0625_s21_qp_23 — Cambridge IGCSE Physics, Paper 2 Multiple Choice (Extended)

**Document:** Cambridge Assessment International Education, Cambridge IGCSE™
PHYSICS 0625/23, Paper 2 Multiple Choice (Extended), May/June 2021, 45 minutes.
The paper itself states "There are forty questions on this paper" and
"The total mark for this paper is 40".

**Converted by:** claude-opus-5, via `agent-conversion` (`/convert`).

## What is in the bundle

- **40 questions**, all extracted — one per printed question, numbered 1–40.
- Pages 1–20 are the question paper (page 1 is the cover/instructions,
  pages 2–18 carry the questions, pages 19–20 are BLANK PAGE / copyright).
- Pages 21–23 are the **published mark scheme** (0625/23 May/June 2021),
  appended by `agent-prepare` with `"role": "answer-key"`.
- **31 figure crops**, every one opened and checked after cropping.
- Every question carries its own `box`. None was left unbounded — no question
  in this paper crosses a page break.

## Where the answers came from

All 40 answers are `"source": "extracted"`, read off the **published mark
scheme** bundled with the paper:

- page 22 of the bundle — the mark-scheme table for questions **1–28**
- page 23 of the bundle — the mark-scheme table for questions **29–40**

Each `answer.evidence` names the mark-scheme page and the letter printed in
that row. The question paper itself carries **no** marks — it is a clean,
unannotated Cambridge paper — so the mark scheme is the only answer source, and
it is unambiguous (one printed capital letter per numbered row, 1 mark each).

Nothing was reasoned and nothing was left blank. No question is flagged.

## Questions whose options are not a printed list

Cambridge prints many of these questions as "Which row is correct?" tables, or
as four lettered drawings, with no option text at all. Per QUALITY.md those
carry `options: ["A","B","C","D"]` and the table/drawing ships as the figure:

- **Q2** — four labelled balls in one drawing (`fig-02`)
- **Q18, Q21, Q25, Q37, Q39** — a single option table
- **Q27** — apparatus diagram **and** option table → two figures
  (`fig-16` diagram, `fig-17` table)
- **Q28** — four labelled magnet/bolt drawings (`fig-18`)
- **Q33** — truth table **and** four gate diagrams → two figures
  (`fig-22` truth table, `fig-23` gate diagrams)
- **Q35** — transformer diagram **and** option table → two figures
  (`fig-25` diagram, `fig-26` table)

No table row was ever flattened into option text.

## Transcription notes / things a tutor may want to glance at

- **Q10** is the one place where the printed layout could not survive as plain
  text. Its four options are printed as stacked fractions
  (numerator over denominator, then `× 100%`). They are transcribed linearly
  with parentheses — e.g. `(E – W) / E × 100%` — which preserves every symbol
  and the option order, but is not pixel-for-pixel the printed form. No figure
  was cropped for it, because the document *did* print a real four-item option
  list; only its typesetting is two-dimensional.
- Superscripts and Greek letters are carried as Unicode where the paper prints
  them: `m / s²`, `1000 kg / m³`, `3.0 × 10⁸ m / s`, `ρ`, `α`, `γ`, `Ω`, `°C`.
  Q39's nuclide notation `²²⁶₈₈Ra` is written with Unicode super/subscripts;
  the printed stacked form is visible in the question's `box`.
- Q19's three numbered hypotheses are kept on their own lines, with blank lines
  separating stem / list / prompt, so the item boundaries survive.
- Cambridge's spacing inside units (`4.0 m / s`, `12.5 kg`, `0.33 Ω`) is
  reproduced as printed rather than normalised.
- The paper states no topic list and no per-question topic headings, so
  `topics` is empty and every question's `topic`/`subtopic` is `""`.
  `year` is `"2021"` for every question (the paper is dated May/June 2021).

## Figures — what was checked

Every crop was written, opened, and re-cut where needed. Adjustments made after
looking: `fig-11`, `fig-14`, `fig-17`, `fig-28` (table borders clipped — widened),
`fig-19` (right-hand label "metal sphere" cut off — widened),
`fig-20` (switch arm clipped — extended),
`fig-21` (resistor box clipped, then a trailing line of the question's own text
bled in — re-cut twice), `fig-27` (magnet pole piece clipped — extended),
`fig-29` (deflected ray running past the right edge — widened).
No crop shows any part of a neighbouring question.

## Left for the tutor

Nothing is outstanding. All 40 answers are filled from the official mark scheme
and should export directly. The only judgement call worth a glance is **Q10**'s
linearised fraction options described above.
