# 0625_m20_qp_22 — Cambridge IGCSE Physics, Paper 2 (Extended)

**February/March 2020, variant 22.** 20 exam pages (page 1 cover, questions on
pages 2–20), plus the published UCLES mark scheme appended as pages 21–23 with
`role: "answer-key"`.

40 questions, numbered 1–40, matching the count the cover states. Every one is
a plain four-option A/B/C/D multiple-choice question — no matching questions,
no true/false grids, no extended-matching stems, nothing that needed splitting
or a `not_mcq` flag.

## Where the answers came from

All 40 from the **appended mark scheme**, which prints one answer letter per
row in a Question / Answer / Marks table (Q1–28 on bundle page 22, Q29–40 on
page 23). Every answer is `source: "extracted"` and its `evidence` names the
key page and row it was read from. Nothing is `reasoned`, nothing is blank, so
the paper exports from Codox with no tutor approvals needed.

The exam pages themselves carry no marks — this is a clean published paper, so
the mark scheme is the sole answer source, and it is complete.

**Independently cross-checked:** all 40 answers were re-read from the mark
scheme PDF's own text layer, a different source from the page images they were
originally read off. All 40 agree.

## Judgement calls

- **15 questions print no option list at all** — the choices are rows of a
  table ("Which row is correct?") or lettered items in a drawing: Q4, 5, 11,
  15, 19, 23, 25, 28, 29, 30, 31, 32, 33, 35, 37. These carry
  `["A","B","C","D"]` with the table or drawing cropped as the figure, per
  QUALITY.md. No table row was flattened into option text.
- **10 questions show two pictures** and got two separate crops, both ids
  listed in printed order: Q4, 5, 15, 19, 23, 24, 28, 29, 33, 35. A diagram and
  its option table are never combined into one crop.
- 42 figures in total; 12 questions need no picture.
- The paper states **no topic taxonomy**, so `topics[]` is empty and every
  `topic`/`subtopic` is blank. Match against your own topic list in Codox after
  import. `year` is `2020` throughout.

## Left for the tutor

Nothing outstanding. Validator: 0 errors, 0 warnings.
